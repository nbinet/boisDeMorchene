import { atom } from 'jotai';

export const socialNetworksAtom = atom(undefined);

export const allSocialNetworksAtom = atom(undefined);

export const selectedSocialNetworkAtom = atom(undefined);